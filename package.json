{
  "name": "failwatch",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "dev": "pnpm --filter @failwatch/web dev",
    "typecheck": "pnpm -r --if-present typecheck"
  }
}
